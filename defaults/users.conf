# Defaults de contas gerenciadas pelo OnePlus.
# Este arquivo é instalado em /etc/oneplus/users.conf na primeira instalação.
DEFAULT_CONNECTION_LIMIT=1
DEFAULT_VALIDITY_DAYS=30
DEFAULT_TEST_HOURS=1
DEFAULT_EXPIRE_ACTION=lock
DEFAULT_TEST_EXPIRE_ACTION=delete-home
